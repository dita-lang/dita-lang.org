name: content-model
inputs:
  INPUT:
    required: true
  OUTPUT:
    required: true
  DIR:
    required: true
  MAP:
    required: true
  SCHEMAS:
    required: true
runs:
  using: 'composite'
  steps:
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'
    - name: Generate content models
      shell: bash
      run: |
        for schema in $(echo "${{ inputs.SCHEMAS }}" | tr ':' '\n'); do
          dir="$(dirname ${schema})"
          file="$(basename ${schema})"
          gradle \
            -Pinput="../${{ inputs.INPUT }}/${schema}" \
            -Poutput="../${{ inputs.DIR }}/${file%%.rng}.dita" \
            -Pschema="${file}"
        done
        echo "Generated:"
        ls ../${{ inputs.DIR }}
      working-directory: dita-lang
    - name: Insert content models
      shell: bash
      run: |
        # Insert to topics
        for i in *.dita; do
          echo "$i"
          name=${i%%.*}
          perl -p -e "s/(<section(?:\s+[\w-]+=[\"'].+?[\"'])*(\s+id=\"attributes\")(?:\s+[\w-]+=[\"'].+?[\"'])*+\s*\/?>)/<section conkeyref=\"content-models-${name}\/${name}.element\" id=\"content-model\"\/>\1/" "$i" > "$i.bak"
          mv "$i.bak" "$i"
        done
        # Insert to map
        for schema in $(echo "${{ inputs.SCHEMAS }}" | tr ':' '\n'); do
          file="$(basename ${schema})"
          perl -p -e "s/(<\/map>)/<mapref href='content-models\/${file%%.rng}.ditamap'\/>\1/" "${{ inputs.MAP }}" > "${{ inputs.MAP }}.bak"
          mv "${{ inputs.MAP }}.bak" "${{ inputs.MAP }}"
        done
        git status
        git diff -- "${{ inputs.MAP }}"
      working-directory: ${{ inputs.OUTPUT }}
